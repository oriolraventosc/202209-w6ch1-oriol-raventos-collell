import {} from "@testing-library/react";

describe("Given a function useAPI", () => {
  describe("When loadTasks is called with new tasks", () => {
    test("Then it should introduce a list of 3 tasks", () => {});
  });
});
